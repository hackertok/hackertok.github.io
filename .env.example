# Optional in local/preview builds. Production releases require the deployed
# Cloudflare Worker origin and validate it during the Vite build.
VITE_PUSH_API_URL=https://hackertok-push.example-account.workers.dev
